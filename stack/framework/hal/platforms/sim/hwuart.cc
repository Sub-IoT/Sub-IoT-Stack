#include "hwuart.h"
#include <assert.h>

void __uart_init()
{
    assert(false);
}
void uart_transmit_data(int8_t data)
{
    assert(false);
}
void uart_transmit_message(void const *data, size_t length)
{
    assert(false);
}
bool uart_tx_ready()
{
    assert(false);
    return false;
}
