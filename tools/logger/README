* download the Qt libraries (> v5.1) for your platfrom (OS and compiler combination) from here: http://qt-project.org/downloads
* install the Qt libraries
* to build using command line:
	$ cd <logger src dir>
	$ qmake && make
* alternatively you can build the project using Qt Creator as well by opening the logger.pro file in Qt Creator.
